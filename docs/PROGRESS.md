# Build progress

| Phase | Status | Commit | Notes |
|---|---|---|---|
| 0 | DONE | fc3c589 | plan + CLAUDE.md |
| 1 | DONE | 56767ca | Astro 7 scaffold (minimal template, TS strict), deps per §1, astro.config.mjs (site/sitemap/trailingSlash), src/config/site.ts, logo moved to src/assets/, favicon.svg (red dot) + regenerated favicon.ico, public/og.png (1200x630, black bg / white wordmark / red dot, built from the source logo via ImageMagick), vitest.config.ts + smoke test. `npm run build`, `npm run check`, `npm run test` all pass. |
| 2 | DONE | ac34583 | Design system + layout. `src/styles/tokens.css` (§3.1 color/type/spacing tokens, dark default + `[data-theme="light"]` override), `src/styles/global.css` (reset, fluid type scale, prose styles, film grain + scanlines overlays, `::selection`, focus ring, `prefers-reduced-motion` global kill-switch), self-hosted fonts wired via `@fontsource-variable/archivo/wdth.css` (wdth+wght axes) and `.../jetbrains-mono/wght.css`. `src/lib/storage.ts` safe localStorage wrapper. `src/layouts/BaseLayout.astro` with inline `is:inline` boot script (sets `data-theme` from storage/`prefers-color-scheme` before paint — no FOUC) + `<ClientRouter />` (confirmed `astro:transitions` API via context7) + SEO meta/OG tags. `Logo.astro` (text wordmark "Podalanga" + red `.dot`, no PNG dependency), `Header.astro` (sticky blurred, nav with active-item dot + `/NN` index, REC pulse dot, live IST clock, ThemeToggle), `Footer.astro` (ouroboros placeholder, GitHub/LinkedIn links, `© 2023–∞`), `ThemeToggle.astro` (instant swap for now — flood-wipe animation is Phase 6). `src/pages/index.astro` now a minimal placeholder hero (real landing content is Phase 8) exercising the layout for QA. Added `scripts/qa.mjs` + `scripts/run-qa.sh` + `npm run qa` (Playwright via installed Chrome, `channel: 'chrome'`, no browser download) per §14.2, ahead of schedule since Phase 2's own Done-when criteria need it. `npm run build`/`check`/`test`/`qa` all pass; qa reports no console errors and no horizontal overflow at 390px in either theme. |
| 3 | DONE | 96ed431 | Content layer + seed. `src/content.config.ts`: `works`/`archive`/`log` collections on the glob loader per §4, with the `archive` schema's photo/video `refine()` checks. All 8 works from §5's table written from the actual `resume_docs/` reports (`pdftotext -layout` + `pdfimages`/`unzip` for figures), each with real dates/org/metrics only (`wheelchair-dynamics` correctly has no cover/figures/metrics — its only source is the resume). `src/config/dossier.ts` for the non-collection Education/Skills/Awards/Positions data. 12 `archive/frame-NNN` entries, one per `test_images/*`, dated from EXIF `DateTimeOriginal` (or the WhatsApp filename date where EXIF was missing), with honest hand-written `alt` text. `log/signal-acquired` welcome post (≤250 words) with one inline image (reused `archive/frame-010`'s photo) and one attachment (`public/media/attachments/signal-acquired.txt`). `npm run build`/`check`/`test` all pass. |
| 4 | DONE | cdd9d45 | Pages (§8). New components: `CaseFileCard`, `WorkMeta`, `Gallery` (grid + JS shortest-column masonry, built-in `<dialog>` lightbox with keyboard/swipe nav), `YouTubeEmbed` (click-to-load `youtube-nocookie.com` facade), `PostCard`, `Attachments` (real file sizes via `fs.statSync` on `public/`), `Tag`, `ContactEmail` (obfuscated — assembled client-side, `<noscript>` "user [at] domain" fallback, per CLAUDE.md's "never the email verbatim in HTML" guardrail, done now rather than deferred to Phase 7 since it's a hard rule, not a phase-gated feature). New pages: `/works` (timeline sorted by start desc, progressive-enhancement kind filter chips, dossier sections), `/works/[slug]` (case-file header via `WorkMeta`, metrics row, cover, prose body, figures `Gallery`, prev/next by `fileNo`), `/archive` (hash-tab nav, all three category sections always stacked in HTML for no-JS, photography masonry, empty-state "NO TRANSMISSIONS RECEIVED" for music/misc since no video entries exist yet), `/log`, `/log/[slug]` (prose, cover, `Attachments`, prev/next by date), `/log/tag/[tag]`, `/404` "UNPERSON" (static mono ASCII eye — no canvas; that's Phase 5), `rss.xml.ts` (`@astrojs/rss`, drafts excluded). Rebuilt `/` with the real §8 landing (hero, featured-works grid, 3-photo archive teaser, latest-3 log entries, contact) — telemetry strip intentionally **not** added (it's `Telemetry.astro`, explicitly Phase 7 work with a live clock/uptime counter; a static stand-in now would just be thrown away later). `BaseLayout` gained a `Person` JSON-LD block on `/` (no email in it, per the same guardrail); per-route SEO already flowed through its existing `title`/`description` props. `Header`'s active-nav-dot matching fixed to `startsWith` so `/works/zbot` etc. still highlight `WORKS`. Added `scripts/check-links.mjs` (+ `npm run check-links`) per §13's "crawl `dist/` for broken internal links" requirement. `npm run build`/`check`/`test`/`check-links` all pass; `npm run qa` (extended to cover `/works`, `/works/zbot`, `/archive`, `/log`, `/log/signal-acquired`, `/404`) reports no console errors and no horizontal overflow at 390px across all 7 routes × 2 themes. |

| 6 | DONE | (this commit) | Theme wipe + page glitch + scramble (§6.2–§6.4). `src/effects/wave.ts` (+`wave.test.ts`, 13 tests): pure `coverDelay`/`revealDelay`/`glitchColumnDelay`/`normalizedDist`/`maxDistanceFromOrigin` — radial per-cell delay from a wipe origin and a horizontal per-column delay for the page-glitch sweep, both bounded and monotonic per §6.2/§6.3. `src/effects/theme-wipe.ts`: click on `#theme-toggle` reads the button's own center as the wave origin, runs the cover pass (glyph flicker in the *target* theme's bg color racing outward, then solid fill; ~0.98s worst case), flips `data-theme` + persists via `lib/storage.ts`, then the reveal pass (flicker in the target signal color, then clears to transparent so the new theme shows through; ~0.47s). Colors for the two themes are small constants mirroring `tokens.css` (the target theme isn't applied to `<html>` yet when the wipe starts, so there's no live computed style to read the target from). Button is `aria-disabled` for the wipe's duration to block re-entry; `aria-pressed`/`aria-label`/icon sync moved out of `ThemeToggle.astro`'s own script (deleted — it was capturing a stale button reference across soft navs, a latent bug from Phase 2) and into `initThemeWipe()`, which re-queries `#theme-toggle` fresh on every `astro:page-load` and tears down on `astro:before-swap`, fixing that staleness for real. `src/effects/page-glitch.ts`: a ~350ms red-glyph band sweeps top→bottom on `astro:before-swap` (columns staggered via `glitchColumnDelay`), masking the ClientRouter DOM swap; registered once for the session (not per-page — it isn't bound to page-specific DOM), with a `setTimeout` failsafe so a stalled rAF can't leave the band on screen. `src/effects/scramble.ts` (+`scramble.test.ts`, 5 tests on the pure `scrambleFrame` helper): `[data-scramble]` elements get their real text moved to `aria-label`, animated glyphs live in an `aria-hidden` child span, resolve left→right over 600ms on first `IntersectionObserver` entry, quick 250ms re-scramble on `mouseenter` of an ancestor `<a>`. Wired to `Header.astro`'s nav labels (`INDEX`/`WORKS`/`ARCHIVE`/`LOG` — safe, non-spoiler content unlike the classified/manifesto text that's explicit Phase-7 scope) as the concrete "hover of links/nav" case from §6.4; no other content opts in yet. All three effects follow the §6.3 `init()`→`destroy()` lifecycle contract: `theme-wipe` and `scramble` touch per-page DOM (the button/nav get recreated on every soft nav) so `BaseLayout`'s bootstrap script re-inits them on every `astro:page-load` and destroys on the next `astro:before-swap`; `page-glitch` is session-global (one listener, set up once). Reduced motion: theme-wipe falls back to a 200ms solid-color opacity crossfade; page-glitch is skipped entirely (default instant swap); scramble elements render their final text immediately with no animation. `npm run build`/`check`/`test`/`check-links` all pass. |
| 7 | DONE | (this commit) | Cryptic layer (§7 items 1–9). `src/effects/terminal/commands.ts` (+`commands.test.ts`, 21 tests): pure parser for `help`/`whoami`/`ls works`/`cat <codename>`/`open <target>`/`contact`/`theme [dark|light]`/`clear`/`date`/`sudo …`/`2+2`/unknown, returning `{ lines, action? }` so `terminal.ts` stays the only DOM-touching half. `src/effects/terminal/terminal.ts` + `src/components/Terminal.astro`: bottom-sheet dialog (`role="dialog"`, focus-trapped between the close button and input — DOM/tab order is close-button-then-input, so the trap wraps at both ends; `tabindex="-1"` on the scrolling log div stops Chromium's auto-scroll-container-focusability quirk from adding a third stop), opens on `` ` ``/`~` (ignored while typing in a field) or the footer hint button, closes on Esc, ↑/↓ history. Site's works serialized to a `<script type="application/json">` at build time per §7.1. `theme` action clicks the real `#theme-toggle` button so it gets the full wipe, not a shortcut. `src/components/Redacted.astro` + `src/effects/redact.ts`: black/`--redact` bar by default, hover/focus/tap scrambles into the real text (reusing `scrambleFrame` from `scramble.ts`) then reverts; accessible name (`aria-label`) is always the real text regardless of visual state. Wired to each work's `classified` phrases (`works/[...slug].astro`, right under the title) and one decorative line in the landing hero (`CLEARANCE <Redacted text="OBSERVER — LEVEL 3" />` — flavor text, not a claimed fact). `src/components/Telemetry.astro` + `src/effects/telemetry.ts`: IST clock, `UPTIME DDDD:HH:MM:SS` since `site.epoch` (new pure `formatUptime()` in `lib/dates.ts`, +3 tests), the two lab coordinates cycling every 4s, a fake SIGNAL bar meter re-rolling every 2.2s; `variant="compact"` reused in the footer, full strip on the landing hero; whole thing `aria-hidden` since it's flavor, not information. `src/components/Ouroboros.astro` + `src/effects/ouroboros.ts`: 20 glyphs placed on a circle at Astro build time (`sin`/`cos`, no canvas needed), rotated as one unit via a single CSS `@keyframes spin` (reduced-motion already zeroes all animation durations globally, so no separate kill-switch needed), JS only pauses it via `IntersectionObserver` when the footer scrolls offscreen. Replaces the Phase 2 placeholder `∞ ∞ ∞` text in `Footer.astro`, which also gained the `press \` to open terminal` hint (`<button data-terminal-open>`) deferred from Phase 2. `src/effects/konami.ts`: ↑↑↓↓←→←→BA (ignored while typing in a field) triggers a 1.5s full-screen red-on-black flash of the three slogans, then fades (reduced motion: instant show/hide, no fade). `src/effects/console-sigil.ts`: logs a small ASCII eye + styled `WE SEE YOU.` signature once. Landing manifesto (headline) gained `data-scramble` for the scramble-in specified by §7 item 9 (email obfuscation §7.7 and the 404 "UNPERSON" §7.8 were already done in Phases 4/4 respectively — confirmed still correct, not re-touched). All of `page-glitch`/`konami`/`console-sigil` are registered once at the top of `BaseLayout`'s body script rather than per-`astro:page-load` — they either only ever touch `document` (which persists across soft navs) or don't touch per-page DOM at all, and the script itself doesn't re-execute on soft navigation (same reasoning Phase 6 used for `page-glitch`); everything else that touches per-page DOM (`theme-wipe`, `scramble`, `redact`, `telemetry`, `ouroboros`, `terminal`) is init'd on `astro:page-load` and torn down on `astro:before-swap`. `npm run test`/`build`/`check`/`check-links` all pass. |
| 8 | DONE | 017d05e | CMS (§9). `public/admin/index.html` (minimal shell, `<meta name="robots" content="noindex">`, pinned `@sveltia/cms@0.212.2` from unpkg loaded as a `type="module"` script with a `sha384` Subresource Integrity hash + `crossorigin="anonymous"` — the plan's own snippet didn't specify SRI, but pinning an exact version on unpkg makes the file content immutable so a hash could be computed and added for defense-in-depth against CDN compromise). `public/admin/config.yml`: GitHub backend targeting `podalanga/podalanga.github.io` (the plan's locked target post-rename, not the current pre-rename `podalanga/podalanga` — §10's step 1 renames the repo, so this is correct for when the CMS is actually used), the three collections (`log`/`archive`/`works`) with fields mapped 1:1 to `content.config.ts`'s Zod schemas (same required/optional split, same enums for `kind`/`category`/`status`, `datetime` widgets pinned to `type: date, format: 'YYYY-MM-DD'` to match `z.coerce.date()`), per-entry co-located media (`media_folder: ''`/`public_folder: ''` + `path: '{{slug}}/index'`) for post/entry-local images matching the existing `./cover.jpg`-style relative paths already used throughout `src/content/`, a dedicated `media_folder: /public/media/attachments` override on the log collection's `attachments` file field (matching the plan's snippet exactly), `works.identifier_field: codename` so CMS-generated slugs follow the existing short-codename convention (`zbot`, not a slugified full title) instead of the default title-based slug. Validated `config.yml` against the real Sveltia JSON schema (`https://unpkg.com/@sveltia/cms/schema/sveltia-cms.json`) via a throwaway `ajv` + `js-yaml` script in `/tmp` (not committed) — valid, only benign "unknown format" warnings for `regex`/`uri` formats Ajv doesn't ship by default. Per §14.5, skipped "Work with Local Repository" (needs a human folder picker) and instead: loaded `/admin/` against `npm run preview` in headless Chrome (`channel: 'chrome'`) via a throwaway Playwright script (not committed) — login screen rendered correctly (Sveltia logo, "Work with Local Repository", "Once prompted, select the root directory of the *podalanga.github.io* repository", "Sign In with GitHub", "Sign In Using Access Token"), zero console/page errors, screenshot reviewed and matches expectations. Hand-wrote one test entry per collection exactly as Sveltia would save them (`log/cms-test-entry` with a copied cover image + a real attachment file under `public/media/attachments/`, `archive/cms-test-video` with `kind: video` + a `youtube` URL, `works/cms-test-work` with all required fields) — `npm run build` succeeded (18 pages, all three test entries rendered: `/log/cms-test-entry/`, `/works/cms-test-work/`, and the video entry appearing in `/archive/`'s markup) — then deleted all three test entries and their media, confirmed `git status --short` showed only the new `public/admin/` as untracked (no residue). Re-ran `npm run build`/`check`/`test`/`check-links` clean at baseline (15 pages, 0 errors, 54 tests, no broken links) before committing. No new npm dependencies added — validation used `npx`-installed throwaway tools in `/tmp`, outside the repo. |
| 9 | DONE | b027f83 | Deploy + docs (§10). `.github/workflows/deploy.yml`: official Astro GitHub Pages recipe verified live via context7 (`/withastro/docs`) — `actions/checkout@v7`, `withastro/action@v6` (`node-version: 22`, matching the engines field), `actions/deploy-pages@v5`; triggers `push: main` + `workflow_dispatch`; `concurrency: { group: pages, cancel-in-progress: false }` added (standard Pages-deploy guard, avoids two concurrent deploys racing) — not in the plan's snippet but harmless/standard. `README.md`: what the site is, stack, `npm run dev/build/preview/check/test/check-links/qa`, how to edit content via the CMS (local-repo mode now; token sign-in once the repo is renamed), and the 4 manual GitHub steps from §10 verbatim, plus the `farmsim_docs` coexistence note. `astro.config.mjs` already had `site: 'https://podalanga.github.io'` and no `base` from Phase 1 (correct for the eventual `podalanga.github.io` user-site rename) — untouched. Per §14.5: did **not** run the workflow (no `act`, and pushing/triggering Actions is forbidden without the owner); instead validated YAML syntax (`npx yaml-lint` on both `deploy.yml` and `public/admin/config.yml`, both pass) and did a clean-clone build: `git clone` into `/tmp` (scratch, outside the repo), `npm ci`, `npm run build` (15 pages, succeeds), `npm run check` (0 errors), `npm run test` (54 tests pass) — scratch clone deleted after. Automated security review flagged the workflow's original top-level `permissions: { contents: read, pages: write, id-token: write }` as broader than each job needs; fixed in a follow-up commit by moving `pages`/`id-token` write down to the `deploy` job only, leaving `build` with `contents: read`. Declined the review's SHA-pinning suggestion for `withastro/action`/`actions/*` since the plan explicitly directs "use the current major versions shown in Astro's GitHub deploy guide," which itself pins by tag, not SHA. |
| 10 | DONE | (this commit) | QA & polish (§13, §14.4). Full end-to-end verification: `npm run check`/`test`/`build`/`check-links` all clean; `grep -rl "@gmail.com" dist`, `find dist -iname "*.pdf"`, `grep -r "test_images\|resume_docs" dist` all empty; `identify -verbose dist/_astro/*.webp \| grep -i 'gps\|exif:make'` no matches (EXIF/GPS stripped); `npm run qa` (extended in prior phases to cover loader/theme-wipe/terminal sub-checks plus all 7 routes × 2 themes × 2 viewports) — zero console/page errors, no horizontal overflow at 390px, all reduced-motion and JS-disabled sub-checks pass. Ran `npx lighthouse --preset=desktop` against `npm run preview` (not previously run in any earlier phase) — Performance/Accessibility/Best-Practices/SEO all 100. Grepped `src`/`public` for stray secrets/API keys/tokens (none) and confirmed `package.json` still matches §1's dependency scope exactly (no undeclared additions across all 10 phases). No defects found needing a fix this phase — everything since Phase 1 was already caught and corrected in its own session (see QA log below for the cross-phase fix history). Visually reviewed 11 screenshots (landing/works/works-zbot/archive/404 across both themes and viewports) directly with the Read tool — consistent red/black case-file language throughout, no generic-portfolio tells, no layout regressions. Wrote `docs/MORNING_REPORT.md` per §14.4 (what was built, how to preview it, the automated verification results, decisions made without the owner, the 4 manual GitHub steps, and a short suggested-review list). Copied and compressed 8 representative screenshots to `docs/preview/*.jpg` (landing dark/light/mobile, works index, works/zbot, archive, log post, 404 — all ≤300KB via ImageMagick resize+requality, committed) so the owner can review on GitHub/phone without running anything. Stopped the preview server and confirmed no leftover `astro`/`node scripts/qa.mjs` processes at session end. Did not run a separate heavy `/code-review` pass: no code changes were needed this phase (only docs/screenshots were added), and every prior phase already went through its own automated-security-review-plus-fix cycle (documented in this file's phase rows — e.g. Phase 7's terminal email leak, Phase 4's lightbox/masonry bugs, Phase 9's workflow permissions) rather than deferring review to the end. |
| 5 | DONE | dc99566 | ASCII engine + Eye loader (§6.1). `src/effects/ascii/grid.ts` (cell sizing `clamp(10px,1.1vw,16px)`, DPR-aware canvas sizing capped at 2, debounced resize), `src/effects/ascii/glyph-atlas.ts` (pre-rendered glyph×brightness-level×color offscreen atlas, `drawImage` per cell — no per-cell `fillText`), `src/effects/eye-field.ts` (pure, unit-tested `intensity(nx, ny, t, state)`: almond lids, edge band, sclera noise, iris striations, pupil void + red dot core, background noise + radiating rays — matches §6.1 exactly), `src/effects/eye-field.test.ts` (12 tests: lid shape, inside/outside eye, pupil growth, edge/sclera/iris/background ranges). `src/effects/eye-loader.ts` orchestrates the full/fast/skip/reduced-motion/failsafe timeline from §6.1's table, RAF-driven, pauses via `document.visibilityState`. `src/components/EyeLoader.astro` renders the static `#eye-loader` overlay (`#eye-canvas`, caption, percent) so content stays in the HTML for SEO/no-JS, and boots `initEyeLoader()` from a module script. `BaseLayout` head boot script now also sets `html.eye-pending` before paint; `global.css` gained the `html.eye-pending body > *:not(#eye-loader) { display: none }` veil plus a `<noscript>` override that reverts it and force-hides `#eye-loader`, so JS-disabled visitors see full content immediately. `npm run qa` extended with `runEyeLoaderChecks()` (`scripts/qa.mjs`): first-visit full sequence sets `pdl:eye-seen` and clears `eye-pending` within budget, repeat visit takes the fast path, click-to-skip short-circuits well under 3.2s, `prefers-reduced-motion` renders a static frame then fades, and a JS-disabled pass confirms `eye-pending` is never left blocking content. `npm run build`/`check`/`test`/`qa` all pass; visually reviewed loader screenshots at t≈1.0s (resolve phase, center-outward reveal) and t≈2.0s (watch phase, caption `OBSERVATION IN PROGRESS · SUBJECT #<hex>` bottom-left + `%` counter bottom-right) — both match §6.1. |

## Decisions made without the owner
- Scaffolded Astro in a scratch dir (`npm create astro` refuses to run in a non-empty directory) then merged the generated `src/`, `public/`, `astro.config.mjs`, `package.json`, `tsconfig.json` into the repo by hand, keeping the existing `CLAUDE.md`, `docs/`, `.gitignore`, and private `resume_docs/`/`test_images/` untouched. The scaffolder's own `AGENTS.md`/`CLAUDE.md` symlink and `README.md` were discarded (ours already exist / will be written in Phase 9).
- `typescript`: plan says pin via `npm view typescript version`, which currently resolves to `7.0.2`. That major is incompatible with `@astrojs/check@0.9.10`'s peer range (`^5.0.0 || ^6.0.0`), so pinned to the latest compatible stable instead: `^6.0.3`.
- `public/og.png` generated programmatically from `podalanga_logo_no_bg.png` with ImageMagick (`convert`, available on the machine): strip the white background, recolor the black wordmark to white (red dot untouched, well outside the color-distance fuzz threshold used), composite centered on a `#0a0a0a` canvas. This is a placeholder-quality OG image; Phase 2/10 (design pass + QA) may want to revisit sizing/typography once the full design system exists.
- `favicon.ico` regenerated from the new `favicon.svg` (red dot on `--bg` dark) via ImageMagick, replacing the scaffold's default Astro rocket icon.
- Ran `npx astro telemetry disable` (local machine setting only, not a repo file) since the plan has no stated preference and this is a personal portfolio project.
- Added `npm run test` (`vitest run`) and a small smoke test (`src/config/site.test.ts`) asserting the nav order / site URL shape, since Phase 1 wires the npm script but the real unit-tested logic (eye-field, wave, terminal commands) doesn't exist until Phases 5–7; an empty vitest run would otherwise fail with "no test files found".

## Known issues / fallbacks
- Phase 10: none blocking — this was a pure verification phase, no code changed. The CMS round-trip and the actual GitHub Actions deploy run remain unattended-unverifiable for the reasons already logged under Phases 8/9 (need a human folder picker / a real token / owner permission to touch Actions); both are called out explicitly in `docs/MORNING_REPORT.md` as the owner's first real-world checks. `npx lighthouse` was run for the first time this phase (not attempted in any earlier phase) since §13 only requires it "if available" — it was available via `npx` and scored 100/100/100/100, so no gap to report.
- Phase 8: none blocking. No real CMS round-trip was possible unattended ("Work with Local Repository" needs a human folder picker, and GitHub sign-in needs a real token) — the owner should do one real round-trip themselves (create/edit a log post with an image and attachment, and a video archive entry, via `npm run dev` → `/admin/` → "Work with Local Repository") to confirm the config end-to-end; noted for `MORNING_REPORT.md` in Phase 10.
- Phase 7: none blocking. One real bug caught and fixed before it ever shipped: `Terminal.astro`'s hidden `#terminal-context` div initially carried the raw site email in a `data-email` attribute — a direct violation of CLAUDE.md's "never the email verbatim in HTML" guardrail (only found by grepping `dist/` per §13, since `npm run check`/`build` don't catch it). Fixed the same way `ContactEmail.astro` already does: split into `data-email-user`/`data-email-domain`, reassembled by `terminal.ts` only when the `contact` command actually runs; re-verified `grep -rl "@gmail.com" dist` is empty after the fix. Also found and fixed a stray `astro preview` server left running from an earlier QA invocation in *this same session* — `scripts/run-qa.sh`'s `kill "$SERVER_PID"` only kills the `npx` wrapper, not `astro preview`'s actual detached daemon (which tracks itself via its own PID file, decoupled from the spawned PID) — so a prior `npm run qa` run left a real server alive on port 4322 even though the script's trap "succeeded". Hardened the cleanup trap to also call `npx astro preview stop`; confirmed no astro/node processes remain after this session's final QA run. One screenshot artifact, not a bug: `qa-artifacts/screenshots/phase7/*.png` (full-page captures) show the terminal panel mid-page instead of off-screen at the bottom — this is a known Playwright/Chromium limitation with `position: fixed` elements during full-page screenshot stitching (verified directly: `getBoundingClientRect()` on `#terminal` in a real 900px viewport gives `top: 900`, i.e. fully below the fold, exactly as `transform: translateY(100%)` intends). The `runTerminalChecks()` sub-checks (which use normal viewport screenshots, not full-page) show it correctly hidden/shown. Minor cosmetic-only nit, not fixed: the comma between multiple `<Redacted>` phrases in a work's classified line can wrap to a new line before the next bar when the line is tight (`works/zbot` has two classified phrases) — functionally fine (each bar independently hoverable/revealable, real text still in the DOM), just a slightly uneven line-wrap; left as-is rather than spending more of the phase budget on a one-page cosmetic wrap.
- Phase 6: none blocking. `page-glitch.ts`'s reduced-motion fallback is "skip the band, let the router's default instant swap happen" rather than a literal fade — Astro's `<ClientRouter />` has no built-in default fade without an explicit `transition:animate` directive on page templates, and adding one project-wide would be new styling scope beyond this phase's brief (a `wave.ts`/`theme-wipe.ts`/`page-glitch.ts`/`scramble.ts` build), so an instant swap was used instead; still satisfies "disable under reduced motion" from the Done-when criteria. `theme-wipe.ts`'s target-theme colors (`bg`/`signal` for dark and light) are duplicated as constants mirroring `tokens.css` rather than read via `getComputedStyle`, because the wipe needs the *target* theme's colors before `data-theme` flips onto `<html>` — there's no live computed style for a theme that isn't applied yet. `scramble.ts` is wired to `Header.astro`'s nav labels only (the one non-spoiler, already-existing piece of content that matches §6.4's own "hover of links/nav" example); the manifesto line and `Redacted.astro` classified phrases that are the other natural `data-scramble` consumers are explicit Phase 7 scope (§7 items 2, 9) and stay untouched so their reveals aren't spoiled early — same reasoning Phase 4 used for `works.classified`.
- None for Phase 1. `npm run build`/`check`/`test` all pass cleanly (0 errors/warnings).
- Phase 2: none — build/check/test/qa all pass. The `eye-pending` FOUC-guard half of the §6.1 boot script (hiding `body > *:not(#eye)` until the loader removes it) is deliberately **not** wired yet, since the `#eye` canvas it depends on doesn't exist until Phase 5 — adding the hide-class now would permanently blank the page. Only the theme half of the boot script (`data-theme` before paint) is live; the eye-pending class will be added alongside the actual loader in Phase 5. Footer's `press \`` to open terminal` hint and real email/mailto (obfuscated) are deferred to Phase 7 when the terminal and obfuscation script actually exist — showing either now would be a dead/broken affordance. Ouroboros footer element is a static `∞` placeholder; the rotating ASCII ring canvas is Phase 7.
- Phase 3: none — `npm run build`/`check`/`test` all pass with the full content set loaded and validated (temporarily wired `getCollection()` into `index.astro` to force-validate all 21 entries during build, confirmed 8/12/1 counts and correct slugs, then reverted `index.astro` to its exact Phase 2 state — `diff` confirmed byte-identical — since page authoring is Phase 4). The `resume_docs/wheelchair-dynamics` work has no report PDF (resume-only per the source table), so it ships with no cover/figures/metrics, matching the plan's own note. Two source files needed workarounds: `IMG20251225070852.jpg` (frame-010) is a mildly corrupt JPEG (`pdftotext`/ImageMagick warn "premature end of data segment") but decodes fine via plain file copy and Astro's `sharp` pipeline handles it; frame-008's subject (a large gilded statue) was recognizable to me as a specific landmark, but per the plan's explicit "leave location empty (don't guess)" instruction for archive seeding, `location` was left unset rather than asserting an identification I wasn't given in metadata.
- Phase 5: none blocking. Fixed one real bug found during self-verification: the static `#eye-loader` markup is part of every page's HTML, so under `<ClientRouter />` soft navigation it was being reinserted on every swap (Astro's router only prevents the *bundled script* from re-running, not the markup swap) — this permanently blacked out the page after the first client-side nav. Fixed by removing `#eye-loader` on `astro:after-swap` for the lifetime of the session in `eye-loader.ts`; confirmed via Playwright across 6 consecutive soft navs with no reappearance and no console errors. Also caught and fixed an inverted resolve-phase reveal direction (was blending in edge-first/outside-in instead of the spec's center-outward) via visual screenshot review, not just the unit tests — the pure field tests alone wouldn't have caught this since threshold *direction* was the bug, not the field values themselves.
- Phase 4: none blocking. `works.classified` (the decorative redacted-phrase intro lines) is written in every work's frontmatter but intentionally **not rendered yet** — `Redacted.astro` is explicit Phase 7 scope (§7 item 2); rendering the phrases in plain text now would spoil the redaction reveal once it exists, so the case-file page simply doesn't show that block yet. Archive's Music/Misc tabs render the "NO TRANSMISSIONS RECEIVED" empty state because the seed content has no video entries (per §5, correctly — CMS/owner can add real `kind: video` entries later and `YouTubeEmbed.astro` is already wired for them). The landing page's telemetry strip is not built yet (see Decisions/Known-issues above) — deferred to Phase 7 with the rest of `Telemetry.astro`.

## QA log (per phase: what was checked, what was fixed)
- Phase 10: see the Phase 10 row above for the full automated-check list (check/test/build/check-links, email/PDF/EXIF/secrets greps, `npm run qa` including its loader/theme-wipe/terminal sub-checks, Lighthouse desktop 100/100/100/100). Visually reviewed `index`, `works`, `works/zbot`, `archive`, `404` screenshots (both themes, desktop + mobile) directly with the Read tool: all read correctly, consistent design language, no regressions, no fixes needed. Curated and compressed 8 of them into `docs/preview/*.jpg` for the owner. No `astro`/QA processes left running at session end.
- Phase 9: `npx yaml-lint` on `.github/workflows/deploy.yml` and (re-check) `public/admin/config.yml` — both pass. Clean-clone build per §14.5: `git clone` this repo into `/tmp/podalanga-clean-clone` (scratch, deleted after), `npm ci` (clean install from lockfile, only an unrelated `undici` engine warning — not a real problem), `npm run build` (15 pages, succeeds), `npm run check` (0 errors/warnings, 53 pre-existing hints), `npm run test` (54/54 pass). This is the first time the repo's install/build has been exercised from a true clean clone (all prior phases only ran in the working tree with `node_modules/` already populated), so it also incidentally confirms nothing is silently depending on local machine state. One fix from automated post-write security review: the workflow's initial top-level `permissions: { contents: read, pages: write, id-token: write }` granted the `build` job (which never touches Pages) the same `pages`/`id-token` write access as `deploy` — moved those two down to a job-level `permissions` block on `deploy` only, re-ran `yaml-lint`, clean. Did not attempt to actually run the workflow (forbidden without the owner per §14.1 rule 5 — no `gh` commands that change anything remote, no push).
- Phase 8: validated `config.yml` against the real Sveltia JSON schema with `ajv` (strict schema pass, no errors). Loaded `/admin/` headlessly against `npm run preview`, confirmed the login screen renders (Sveltia logo, repo name "podalanga.github.io" correctly interpolated into the local-repo hint text, both sign-in options) with zero console/page errors; reviewed the screenshot directly with the Read tool. Round-tripped one hand-written test entry per collection through `npm run build` (log post with cover image + attachment file, archive video entry, works entry) — all three rendered into `dist/` correctly, then deleted; confirmed via `git status --short` that no test-entry residue was left behind and the build/check/test/check-links suite is clean at the pre-Phase-8-content baseline (only `public/admin/` is new). No stray `astro preview` process left running (`astro preview stop` + `ps aux` check).
- Phase 7: extended `scripts/qa.mjs` with `runTerminalChecks()`: backtick opens the terminal and focuses its input, `help`/`ls works`/`cat zbot` produce the expected output (checked by content, not just "no error"), Tab cycles correctly within the close-button/input focus trap (wrapping both directions), `open archive` actually navigates, Esc closes, a redacted element's `aria-label` carries the real text (SR-accessible) independent of its hover state, and the Konami sequence produces the flash overlay. Two critique→fix iterations from the actual first runs (not just guessed): (1) all four DOM-interaction checks failed with an empty `document.activeElement.id` because the check pressed the open key only 200ms after `goto()` — too early for even the *fast* eye-loader timeline (~0.9s) to finish, so the terminal panel was still hidden under `html.eye-pending`'s `display:none` veil and couldn't receive focus; fixed by waiting on the same `eye-pending` class removal the other checks already use, in all four sub-blocks. (2) the focus trap's forward-Tab-from-input assertion failed because I'd assumed DOM order `[input, closeButton]` when the actual markup has the close button first (in the top `.bar`) and the input last (in the bottom `.input-row`) — fixed the trap's `focusables` array to match real DOM order, which also surfaced a third, unrelated bug: Chromium auto-adds scrollable `overflow-y: auto` regions (`#terminal-log`) as an implicit tab stop for keyboard scrolling, so "Tab from close button" landed on the log div instead of the input; fixed with an explicit `tabindex="-1"` on that div. Ran `npm run test`/`build`/`check`/`check-links`/`qa` clean after all three fixes. Visually reviewed the landing page and `works/zbot` screenshots (both themes, both viewports) against §3: telemetry strip reads correctly in the header rhythm, the redacted CLEARANCE/classified bars render as solid opaque bars matching `--redact` in both themes with no leaked text, footer's new terminal hint and Ouroboros ring don't crowd the existing GitHub/LinkedIn links or copyright line at 390px, nav headline's new scramble-in doesn't reflow (same character count, only glyphs change). Also grepped built `dist/` for the raw email per §13 (`grep -rl "@gmail.com" dist`) — this is what caught the `data-email` leak described above; re-ran after the fix and confirmed empty. Confirmed no leftover `astro`/`node scripts/qa.mjs` processes at session end (`ps aux`), including cleaning up one process left over from mid-session QA debugging that predated the `run-qa.sh` cleanup-trap hardening described above.
- Phase 6: extended `scripts/qa.mjs` with `runThemeWipeChecks()`: mid-flood (~300ms) and after screenshots for both toggle directions, theme persists across `reload()`, reduced-motion click still changes `data-theme`, JS-disabled render (button present, no error), and a 10-soft-navigation loop (`/works`→`/archive`→`/log`→`/`×2.5) ending with a working toggle click and at most one lingering `<canvas>` in the DOM — confirming no leaked overlay canvases/listeners from `theme-wipe`/`page-glitch`/`scramble`'s init/destroy lifecycle. One critique→fix iteration from visually reviewing screenshots with the Read tool: `index__desktop__dark.png` showed the header nav labels frozen mid-scramble (`IN\??`, `WO\_-`, `ARC\!^*`, `>&[` instead of `INDEX`/`WORKS`/`ARCHIVE`/`LOG`) — traced with a throwaway debug script (added a temporary `console.log` in `scramble.ts`'s `start()`, confirmed via `page.on('console')` it fired exactly once, ~972ms after navigation) to a QA-harness timing gap, not an effect bug: `shootRoute()`'s scroll-through-page step only takes ~180ms for a page this tall, so the screenshot fired well before the 600ms scramble-in animation finished — a real user would simply see it resolve, but the automated screenshot caught it mid-flight. Fixed by adding a 700ms wait after `eye:done` in `shootRoute()` (covers the 600ms `RESOLVE_MS` plus margin) before scrolling/capturing; re-shot `npm run qa` end-to-end — all nav labels resolved correctly across every route/theme/viewport, debug logging removed from `scramble.ts`. Reviewed the theme-wipe mid-flood screenshots directly: the flood originates visibly from the toggle button's corner and radiates outward as intended (confirmed both directions — Chromium's default emulated `prefers-color-scheme` is `light`, so the first click in the QA run is actually light→dark despite the screenshot filenames assuming the opposite starting theme; harmless naming quirk, not a defect, since the assertions are theme-value-based not color-based). `npm run build`/`check`/`test`/`check-links`/`qa` all pass; no stray `astro dev`/`preview` processes left running (checked via `ps aux` at session end).
- Phase 1: ran `npm run build`, `npm run check`, `npm run test` — all pass. Verified `git status` shows only the intended new files staged (no `node_modules/`, `dist/`, `.astro/`, `resume_docs/`, or `test_images/` leaked past `.gitignore`). Visually reviewed `public/og.png` with the Read tool — legible wordmark + red dot on black, no artifacts from the background-removal step.
- Phase 2: built `scripts/qa.mjs`/`scripts/run-qa.sh` (Playwright + installed Chrome, `channel: 'chrome'`, no browser download — per §14.2) and ran `npm run qa` against `/` at 1440×900 and 390×844, both themes. First pass: layout, hairlines, nav active-dot, sticky blurred header, and both theme palettes all read correctly with no console errors and no horizontal overflow at 390px. One critique→fix iteration: the placeholder Ouroboros glyph run (`◜◝◞◟…`) read as a messy broken squiggle rather than an intentional placeholder — swapped for a plain `∞ ∞ ∞…` run that echoes the perpetuity motif already in the copyright line; re-shot and confirmed. Reviewed both themes for contrast (`--fg-muted` on `--bg` in both palettes) and confirmed no generic "AI portfolio" tells (no cream+serif+terracotta, no near-black+neon accent, no rounded SaaS cards) — matches the locked red/black case-file direction in §3.
- Phase 3: no browser QA yet (no pages consume the content — that's Phase 4). Verification here was `npm run build`/`check`/`test` plus a temporary `getCollection()` probe in `index.astro` (added, run, then reverted — confirmed byte-identical to the pre-probe file via `diff`) to force schema validation and print collection sizes/slugs: `works=8 archive=12 log=1`, with the exact slugs from §5's table (`zbot`, `rotary-inverted-pendulum`, `wheelchair-dynamics`, `ros2-differential-drive`, `rovio-underwater-rov`, `3r-robotic-arm`, `balancing-builder-bot`, `pla-filament-extruder`) and `frame-001`…`frame-012`. Figures were curated by extracting every embedded image from each PDF/PPTX with `pdfimages`/`unzip`, building contact-sheet montages with ImageMagick, and reading each candidate with the Read tool before selecting 2–4 relevant, non-redundant figures per work (renders, hardware photos, and the most information-dense plot/diagram per report) — no boilerplate club-logo or blank-slide images were kept. `npm run test` (vitest) still passes (2 tests, unrelated to content).
- Phase 4: ran `npm run build`/`check`/`test` (0 errors/warnings) and `scripts/check-links.mjs` (new, per §13) against all 15 built HTML files — no broken internal `href`/`src`. Confirmed `dist/` has no `.pdf`, no `test_images`/`resume_docs` path leaks, and (sampled) no GPS/EXIF `Make` tags surviving Astro's `sharp` pipeline. Extended `scripts/qa.mjs`'s route list to `/`, `/works`, `/works/zbot`, `/archive`, `/log`, `/log/signal-acquired`, `/404` and ran `npm run qa` (both themes, both viewports = 28 shots): first pass surfaced two real bugs caught only by looking at the screenshots — (1) the `Gallery` lightbox `<dialog>` was rendering permanently open/dimmed on every page load, because its scoped `display: grid` rule had higher specificity than the browser's default `dialog:not([open]) { display: none }` and unconditionally won; fixed by gating all of the dialog's layout CSS behind `.lightbox[open]`. (2) the archive photography masonry (native CSS `column-count` + `break-inside: avoid`) hit a real Chromium multi-column balancing bug — tall images got pushed to the next column while leaving large blank gaps in the current one; replaced it with a small JS shortest-column placement algorithm (measures each frame's already-known intrinsic height from its `width`/`height` attrs, no need to wait for image bytes; degrades to a static full-width stack via a `:not(.js-laid-out)` CSS rule when JS is unavailable). Re-shot after both fixes: clean. Also found the QA script's own full-page screenshots were racing native `loading="lazy"` — the last-placed masonry images looked blank because their network fetch hadn't finished before the screenshot fired; fixed by having `qa.mjs` scroll through the full page before capturing. Separately ran a one-off Playwright check with `javaScriptEnabled: false` against `/`, `/works`, `/works/zbot`, `/archive`, `/log/signal-acquired` — nav text and all expected `<img>` counts present on every route, confirming the no-JS requirement (images, prose, nav all work without JS; only the lightbox/tab-highlighting/filter-chip interactions are JS-only enhancements). Reviewed both themes against §3 (grid/hairlines, red-dot motif, mono section labels, red duotone→color hover on archive thumbnails) — no generic-portfolio tells. `astro preview` background process stopped at end of session (`astro preview stop`).
